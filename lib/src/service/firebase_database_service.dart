class FirebaseDatabaseService {
  Future<void> saveRealTimeRecord(
      String ridingDate, num ridingTime, num distance) async {}
  // Future<void> saveFinalRecord(
  //     String ridingDate, num ridingTime, num distance) async {}
}
